# Public storefront (mvst.tsumteam.ru) — production
API_DOMAIN=api.tsum.ru
SSR_API_DOMAIN=https://api.tsum.ru

# CRM ingest endpoint (intra-docker hostname; matches service name in docker-compose)
CRM_URL=http://mvst-crm:3001
CRM_INGEST_KEY=replace-with-strong-shared-secret
